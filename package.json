{
  "name": "ai-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Shared local-AI enrichment service (Ollama) for flat-finder + Personal-Site vacancies. Deterministic parsers stay in the apps; this is the LLM enrichment layer.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/queue/queue.js && node --check src/prompts/candidate.js && node --check src/ollama/client.js",
    "test": "node --test",
    "benchmark": "node scripts/benchmark.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "zod": "^3.24.1"
  }
}
